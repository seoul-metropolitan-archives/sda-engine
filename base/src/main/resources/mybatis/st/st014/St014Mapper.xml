<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="rmsoft.ams.seoul.st.st014.dao.St014Mapper">
    <select id="getStWithoutNoticeInoutRecord" resultType="St01401VO" parameterType="St01401VO" statementType="PREPARED">
        SELECT
               inout_record.AGGREGATION_UUID aggregationUuid,
               inout_record.WITHOUT_NOTICE_IO_RECORD_UUID withoutNoticeIoRecordUuid,
               B.code,
               B.title,
               B."level",
               B."type",
               B.publishedStatus,
               B.author,
               B.descStrDate,
               B.descEdDate,
               repository.REPOSITORY_NAME repositoryName,
               shelf.SHELF_NAME shelfName,
               (location.ROW_NO || '행 ' || location.COLUMN_NO || '열') as locationName,
               inout_hist.INOUT_DATE_TIME inoutDateTime
        FROM ST_WITHOUT_NOTICE_INOUT_RECORD inout_record
        INNER JOIN ST_WITHOUT_NOTICE_INOUT_HIST inout_hist
            ON inout_hist.WITHOUT_NOTICE_IO_RECORD_UUID = inout_record.WITHOUT_NOTICE_IO_RECORD_UUID
        LEFT JOIN ST_ARRANGE_RECORDS_RESULT etc
          ON etc.AGGREGATION_UUID = inout_record.AGGREGATION_UUID
        LEFT JOIN ST_ARRANGE_CONTAINERS_RESULT acr
            ON acr.CONTAINER_UUID = etc.CONTAINER_UUID
        LEFT JOIN ST_LOCATION location
            ON location.LOCATION_UUID = acr.LOCATION_UUID
        LEFT JOIN ST_CONTAINER container
            ON container.CONTAINER_UUID = etc.CONTAINER_UUID
        LEFT JOIN ST_SHELF shelf
            ON shelf.SHELF_UUID = location.SHELF_UUID
        LEFT JOIN ST_REPOSITORY repository
            ON repository.REPOSITORY_UUID = shelf.REPOSITORY_UUID
        LEFT JOIN (SELECT AGGREGATION_UUID                                                                         as uuid,
                             1                                                                                        as orderNo,
                             (SELECT CODE_NAME
                              FROM AD_CODE_DETAIL
                              WHERE CODE_DETAIL_UUID = Z1.PUBLISHED_STATUS_UUID)                                      as publishedStatus,
                             AGGREGATION_CODE                                                                         as code,
                             TITLE                                                                                    as title,
                             (SELECT CODE_NAME
                              FROM AD_CODE_DETAIL
                              WHERE CODE_DETAIL_UUID = Z1.TYPE_UUID)                                                  as "type",
                             (SELECT LEVEL_NAME
                              FROM RC_LEVEL
                              WHERE LEVEL_UUID = Z1.LEVEL_UUID)                                                       as "level",
                             AUTHOR                                                                                   as author,
                             DESCRIPTION_START_DATE                                                                   as descStrDate,
                             DESCRIPTION_END_DATE                                                                     as descEdDate,
                             DESCRIPTION                                                                              as description,
                             NOTES                                                                                    as notes,
                             ((SELECT COUNT(*) FROM RC_AGGREGATION WHERE PARENT_AGGREGATION_UUID = Z1.AGGREGATION_UUID) +
                              (SELECT COUNT(*)
                               FROM RC_ITEM
                               WHERE AGGREGATION_UUID = Z1.AGGREGATION_UUID))                                         as childCnt
                      FROM RC_AGGREGATION Z1
                      UNION ALL SELECT ITEM_UUID                                                                    as uuid,
                                       2                                                                            as orderNo,
                                       (SELECT CODE_NAME
                                        FROM AD_CODE_DETAIL
                                        WHERE CODE_DETAIL_UUID = Z1.PUBLISHED_STATUS_UUID)                          as publishedStatus,
                                       ITEM_CODE                                                                    as code,
                                       TITLE                                                                        as title,
                                       (SELECT CODE_NAME
                                        FROM AD_CODE_DETAIL
                                        WHERE CODE_DETAIL_UUID = Z1.TYPE_UUID)                                      as "type",
                                       ''                                                                           as "level",
                                       AUTHOR                                                                       as author,
                                       DESCRIPTION_START_DATE                                                       as descStrDate,
                                       DESCRIPTION_END_DATE                                                         as descEdDate,
                                       DESCRIPTION                                                                  as description,
                                       NOTES                                                                        as notes,
                                       0                                                                            as childCnt
                                FROM RC_ITEM Z1) B on B.UUID = inout_record.AGGREGATION_UUID
        <where>
            1=1
            <if test="code != null">
                <![CDATA[
                   AND B.code  LIKE '%' || #{code} || '%'
                ]]>
            </if>
            <if test="title != null">
                <![CDATA[
                   AND B.title  LIKE '%' || #{title} || '%'
                ]]>

            </if>
            <if test="repositoryUuid != null">
                AND  repository.REPOSITORY_UUID = #{repositoryUuid}
            </if>
            <if test="shelfUuid != null">
                AND  shelf.SHELF_UUID = #{shelfUuid}
            </if>
            <if test="inoutDateTimeFrom != null">
                AND inout_hist.INOUT_DATE_TIME <![CDATA[>=]]> TO_DATE(#{inoutDateTimeFrom},'yyyy-mm-dd')
            </if>
            <if test="inoutDateTimeTo != null">
                AND inout_hist.INOUT_DATE_TIME <![CDATA[<]]> TO_DATE(#{inoutDateTimeTo},'yyyy-mm-dd')
            </if>
        </where>
    </select>

    <select id="getStWithoutNoticeInoutHist" resultType="St01402VO" parameterType="St01402VO" statementType="PREPARED">
        SELECT hist.WITHOUT_NOTICE_IO_RECORD_UUID, hist.INOUT_DATE_TIME inoutDateTime, gate.ZONE_NAME zoneName FROM ST_WITHOUT_NOTICE_INOUT_HIST hist
          INNER JOIN (
            SELECT * FROM ST_GATE gate
              INNER JOIN ST_ZONE zone
                ON gate.OUT_ZONE_UUID = zone.ZONE_UUID
          ) gate
          ON hist.GATE_UUID = gate.GATE_UUID
        WHERE 1 = 1
          AND hist.WITHOUT_NOTICE_IO_RECORD_UUID = #{withoutNoticeIoRecordUuid}
    </select>
</mapper>
