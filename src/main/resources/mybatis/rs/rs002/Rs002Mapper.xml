<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="rmsoft.ams.seoul..Rs002Mapper">

    <select id="findAll" resultType="rs002" parameterType="rs002" statementType="PREPARED">
        SELECT
            TRIGGER_UUID AS triggerUuid,
            STATUS_UUID AS statusUuid,
            TRIGGER_CODE AS triggerCode,
            TRIGGER_NAME AS triggerName,
            TRIGGER_DATE AS triggerDate,
            DESCRIPTION AS description,
            NOTES AS notes,
            USE_YN AS useYn,
            INSERT_UUID AS insertUuid,
            INSERT_DATE AS insertDate,
            UPDATE_UUID AS updateUuid,
            UPDATE_DATE AS updateDate
        FROM
            RS_TRIGGER
    </select>

    <select id="findOne" resultType="rs002" parameterType="rs002" statementType="PREPARED">
        SELECT
            TRIGGER_UUID AS triggerUuid,
            STATUS_UUID AS statusUuid,
            TRIGGER_CODE AS triggerCode,
            TRIGGER_NAME AS triggerName,
            TRIGGER_DATE AS triggerDate,
            DESCRIPTION AS description,
            NOTES AS notes,
            USE_YN AS useYn,
            INSERT_UUID AS insertUuid,
            INSERT_DATE AS insertDate,
            UPDATE_UUID AS updateUuid,
            UPDATE_DATE AS updateDate
        FROM
            RS_TRIGGER
        WHERE
            TRIGGER_UUID = #{triggerUuid}
    </select>

   <update id="delete" parameterType="rs002" statementType="PREPARED">
        DELETE FROM
            RS_TRIGGER
        WHERE
            TRIGGER_UUID = #{triggerUuid}
    </update>

   <update id="update" parameterType="rs002" statementType="PREPARED">
        UPDATE RS_TRIGGER
        SET
            STATUS_UUID = #{statusUuid},
            TRIGGER_CODE = #{triggerCode},
            TRIGGER_NAME = #{triggerName},
            TRIGGER_DATE = #{triggerDate},
            DESCRIPTION = #{description},
            NOTES = #{notes},
            USE_YN = #{useYn},
            INSERT_UUID = #{insertUuid},
            INSERT_DATE = #{insertDate},
            UPDATE_UUID = #{updateUuid},
            UPDATE_DATE = #{updateDate}
        WHERE
            TRIGGER_UUID = #{triggerUuid}
    </update>

   <update id="insert" parameterType="rs002" statementType="PREPARED">
        INSERT INTO RS_TRIGGER (
            TRIGGER_UUID,
            STATUS_UUID,
            TRIGGER_CODE,
            TRIGGER_NAME,
            TRIGGER_DATE,
            DESCRIPTION,
            NOTES,
            USE_YN,
            INSERT_UUID,
            INSERT_DATE,
            UPDATE_UUID,
            UPDATE_DATE
        ) VALUES (
            #{triggerUuid},
            #{statusUuid},
            #{triggerCode},
            #{triggerName},
            #{triggerDate},
            #{description},
            #{notes},
            #{useYn},
            #{insertUuid},
            #{insertDate},
            #{updateUuid},
            #{updateDate}
        )
    </update>
</mapper>