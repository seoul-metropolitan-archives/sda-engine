<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bgf.shbank.domain.mng.etc.sh04001180.Sh04001180Mapper">

    <select id="findAll" resultType="sh04001180" parameterType="sh04001180" statementType="PREPARED">
        SELECT
            TX_ID AS txId,
            JISA_CODE AS jisaCode,
            BRANCH_CODE AS branchCode,
            CORNER_CODE AS cornerCode,
            CRT_NO AS crtNo
        FROM
            ATMS_04001180
    </select>

    <select id="findOne" resultType="sh04001180" parameterType="sh04001180" statementType="PREPARED">
        SELECT
            TX_ID AS txId,
            JISA_CODE AS jisaCode,
            BRANCH_CODE AS branchCode,
            CORNER_CODE AS cornerCode,
            CRT_NO AS crtNo
        FROM
            ATMS_04001180
        WHERE
            TX_ID = #{txId}, BRANCH_CODE = #{branchCode}, CORNER_CODE = #{cornerCode}
    </select>

   <update id="delete" parameterType="sh04001180" statementType="PREPARED">
        DELETE FROM
            ATMS_04001180
        WHERE
            TX_ID = #{txId}, BRANCH_CODE = #{branchCode}, CORNER_CODE = #{cornerCode}
    </update>

   <update id="update" parameterType="sh04001180" statementType="PREPARED">
        UPDATE ATMS_04001180
        SET
            JISA_CODE = #{jisaCode},
            CRT_NO = #{crtNo}
        WHERE
            TX_ID = #{txId}, BRANCH_CODE = #{branchCode}, CORNER_CODE = #{cornerCode}
    </update>

   <update id="insert" parameterType="sh04001180" statementType="PREPARED">
        INSERT INTO ATMS_04001180 (
            TX_ID,
            JISA_CODE,
            BRANCH_CODE,
            CORNER_CODE,
            CRT_NO
        ) VALUES (
            #{txId},
            #{jisaCode},
            #{branchCode},
            #{cornerCode},
            #{crtNo}
        )
    </update>
</mapper>